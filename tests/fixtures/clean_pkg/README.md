# cleanlib

A tiny, benign library. It only does arithmetic and string formatting.

## Configuration

Copy `.env.example` to `.env` to set optional flags. The `.env` file is documentation
only here; this library never reads it. (Mentioning `.env` in prose must not be flagged.)
