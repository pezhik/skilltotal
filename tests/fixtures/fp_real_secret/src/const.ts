// Counter-fixture: a real-shaped provider key (AWS) sitting next to DocSearch-shaped noise.
// A known-prefix provider secret must STILL be flagged — never swept up by the DocSearch allowlist.
export const CONFIG = {
  indexName: "agentguard",
  awsAccessKeyId: "AKIA2E0N9KL7P3QR8XYZ",
};
