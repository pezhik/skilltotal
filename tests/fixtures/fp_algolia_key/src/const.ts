// Algolia DocSearch (public, search-only) credentials for the documentation site.
// These are read-only keys meant to be shipped in client-side code, not a leaked secret.
// (Synthetic value — shape only — so the engine's own secret scanner has nothing real to find.)
export const DOCS_SEARCH = {
  appId: "N8EOCSBQGH",
  apiKey: "a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
  indexName: "agentguard",
};
